---
description: CORE_UX kit components — core-* tags, base classes, doc maintenance, presentation-only
globs: **/*.js
alwaysApply: false
---

# CORE_UX — components

Generic `<core-*>` presentation on **CORE_JS** (`Core_HTMLElement`). Details: `ai-instructions/components.md`.

## Role (mandatory)

- Reusable UI kit — **no business logic**, **no `$svc('ajax')`**, no app routes/entities.
- Renders with **`ui_render()`** (pure DOM) — **no Handlebars** in this repo.

## Lifecycle (Core_HTMLElement — daily minimum)

1. **`onConnect()`** — `addSub()` for long-lived streams, then `this.render()`.
2. **`render()`** — `cleanFunctional()` → `ui_render()` → `$svc('lang').process(this)` if lang active → `ui_toFunctional()`.
3. **`ui_toFunctional()`** — `bindDelegated()` / `bindUI()` only; never manual `addEventListener`.
4. **`cleanFunctional()` override** — call `super.cleanFunctional()` first.

Optional deep dive (sibling repo): `CORE_JS/ai-instructions/components.md`.

## Base classes

| Class | Use |
|-------|-----|
| `Core_UXElement` | Default base |
| `Core_UXSlotElement` | Preserves light-DOM children (modal, card, …) |
| `Core_UXFormControl` | Labelled inputs |

## Conventions

- Tag names: `core-<name>` (kebab-case).
- Register: `registerCoreComponent('core-<name>', Class)` at module bottom.
- Boolean attrs: `hasBoolAttr`; JSON attrs: `parseJsonAttr`.
- DOM: `createElement({ text })` for user/API strings; `trustedHtml` / `mountTrustedHtml` for author markup only.

## UX contract

- Component APIs are semantic: expose and document labels, hints, errors, states, keyboard behavior, and accessible names where relevant.
- Supported attribute values are explicit and documented; do not rely on silent fallback as an app convention.
- Every interactive component must work by keyboard, retain visible focus, and avoid color-only meaning.
- Every `aria-modal="true"` surface requires a non-empty accessible name and owns initial focus, Tab/Shift+Tab containment, Escape handling, cleanup, and safe focus restoration.
- Overlay attribute syncs must distinguish closed-to-open from open-to-open updates; simple visual/name changes must not refocus or rebuild stateful slot content.
- Generic layout or state patterns belong here only when reusable unchanged; product hierarchy and wording stay in the app.
- Composition/state guidance: `ai-instructions/ux-quality.md`.

## Styles & doc (same change set)

- Tailwind in `styles/core-ux.css` — **core-* token utilities only** — see `core-ux-theming.mdc`.
- **Mandatory:** update `doc/components/core-<name>.html` + `doc/assets/sidebar.js` when adding/changing tags.

## App consumption

Apps import subsets — not `index.js` barrel. See `ai-instructions/app-integration.md`.
